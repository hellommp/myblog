<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianli.dao.UserDao">
	
    <resultMap type="com.tianli.entity.User" id="userBean">
		<id column="uid" property="id"/>
		<result column="uname" property="name"/>
		<result column="password" property="passWord"/>
		
		<collection property="myBlogList" ofType="com.tianli.entity.Blog">
			<id column="bid" property="id"/>
			<result column="title" property="title"/>
			<result column="publishdate" property="publishDate"/>
			
			<association property="category" javaType="com.tianli.entity.Category">  
            	<result column="cid" property="id"/>  
            	<result column="cname" property="name"/>  
        	</association>  
        	
        	<association property="keyWord" javaType="com.tianli.entity.KeyWord">  
            	<result column="kid" property="id"/>  
            	<result column="kname" property="name"/>  
        	</association>  
        
		</collection>
	</resultMap>
	
	<select id="queryUserByName" parameterType="string" resultMap="userBean">
	SELECT 
		* 
	FROM 
		user
	WHERE
		name=#{name}
	</select>
	
	<select id="queryUserById" parameterType="int" resultMap="userBean">
	SELECT
		u.id 'uid',u.name 'uname',u.password,
		b.id 'bid',b.title,b.publishdate,
		c.id 'cid',c.name 'cname',
		k.id 'kid',k.name 'kname'
	FROM
		user u,blog b,category c,keyword k
	WHERE
		uid = #{id} 
	AND
		b.uid = #{id}
	AND
		b.cid = c.id
	AND
		b.kid = k.id
	</select>
	
	

</mapper>