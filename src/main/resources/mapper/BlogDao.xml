<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianli.dao.BlogDao">
	<insert id="addBlog" parameterType="com.tianli.entity.Blog">
		INSERT INTO
			blog(title,publishdate,cid,keyword,uid,content)
		VALUES
		(#{title},#{publishDate},#{category.id},#{keyWord},#{user.id},#{content})
	</insert>
	
	<delete id="deleteBlog" parameterType="int">
		DELETE FROM
			blog
		WHERE 
			id = #{id}
	</delete>

	<update id="refreshBlog">
		UPDATE 
			blog
		SET
			content = #{content}
		WHERE
			id = #{id}
	</update>
	
	<select id="queryBlogById" parameterType="int" resultType="com.tianli.entity.Blog">
		SELECT 
			*
		FROM
			Blog
		WHERE
			id = #{id}	
	</select>
</mapper>